# Project Phoenix demo — dependencies
# Python 3.11+

# MCP servers (sub-agents) + client
fastmcp>=2.0

# LLM providers (install at least one; default is gemini) — Layer B only,
# NOT used on the Layer-A happy path.
google-genai>=1.20        # Gemini  (PHOENIX_LLM=gemini, GEMINI_API_KEY)
anthropic>=0.40           # Claude  (PHOENIX_LLM=claude, ANTHROPIC_API_KEY)

# Phase 1 — orchestration (LangGraph workflow engine)
langgraph>=0.2
langchain-core>=0.3

# Phase 2 — WebSocket bridge + sequence prototype UI
fastapi>=0.110
uvicorn[standard]>=0.29   # includes websockets

# Environment variables loading
python-dotenv>=1.0.0
